{
  "name": "<%= appName %>",
  "version": "0.0.1",
  "description": "My awesome app",
  "private": true,
  "repository":  "<%= repoUrl %>",
  "main": "server.app.js",
  "scripts": {
    "start": "fly",
    "test": "fly test",
    "lint": "fly lint",
    "build": "NODE_ENV=production fly build",
    "build:try": "NODE_ENV=production fly tryBuild",
    "deploy": "scripts/deploy"
  },
  "dependencies": {},
  "devDependencies": {}
}
